"""
Demo panels for the RSC-SIM Streamlit app. Each module exposes a `render()`
function that draws one tab's controls and outputs.

These panels rely on `demo_app.sim_cache`, which `app.py` ensures is importable
by prepending `demo_app/` to `sys.path` before any panel import.
"""
